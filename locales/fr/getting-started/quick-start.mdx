---
title: Guide de Démarrage Rapide
description: Soyez opérationnel avec Apprise en quelques minutes.
sidebar:
  order: 3
---

import { Tabs, TabItem } from "@astrojs/starlight/components";

Maintenant qu'Apprise est installé, envoyons votre première notification. Choisissez la méthode qui vous convient le mieux ci-dessous.

<Tabs>
<TabItem label="CLI">

La façon la plus rapide de tester Apprise est d'utiliser la ligne de commande.

```bash
# Envoyer une notification vers un service spécifique
apprise -t "Bonjour" -b "Monde" \
    "discord://webhook_id/webhook_token"
```

</TabItem>
<TabItem label="Python">

Si vous êtes développeur, voici comment utiliser Apprise dans un script Python :

```python
import apprise

# Créer une instance Apprise
apobj = apprise.Apprise()

# Ajouter des services de notification via des URL
apobj.add('mailto://user:pass@gmail.com')
apobj.add('discord://webhook_id/webhook_token')

# Envoyer une notification
apobj.notify(
    body='Ceci est une notification de test !',
    title='Bonjour le monde',
)
```

</TabItem>
<TabItem label="Serveur API">

Assurez-vous que votre conteneur API Apprise est en cours d'exécution (voir [Installation](/getting-started/installation/)).

### 1. Sans État (Le Plus Rapide)

Vous pouvez envoyer une notification immédiatement sans rien configurer sur le serveur en passant les URL dans la requête.

```bash
# Envoyer une notification via curl
curl -X POST \
  -d 'urls=discord://webhook_id/webhook_token' \
  -d 'body=Bonjour le monde' \
  http://localhost:8000/notify
```

### 2. L'Interface Web

L'API inclut un gestionnaire de configuration intégré à l'adresse `http://localhost:8000`.

#### Étape 1 : Choisir une Clé

Les configurations sont stockées sous un **ID de configuration** unique (ou clé). Choisissez un mot-clé (par exemple `mes-alertes`) pour commencer.

![Connexion à l'API Apprise](./images/api-screen-1.png)

#### Étape 2 : Ajouter une Configuration

Saisissez vos URL Apprise (au format TEXT ou YAML) et enregistrez-les sous votre clé.

![Configuration de l'API Apprise](./images/api-screen-2.png)

#### Étape 3 : Vérifier et Étiqueter

Vérifiez que vos URL sont bien chargées. Vous pouvez voir les **tags** qui leur sont associés afin de cibler certains groupes plus tard.

![Vérification de l'API Apprise](./images/api-screen-3.png)

#### Étape 4 : Envoyer une Notification

Passez à l'onglet **Notifications**. Sélectionnez les tags à cibler (ou laissez vide pour notifier tout le monde), puis envoyez votre notification.

![Envoi via l'API Apprise](./images/api-screen-4.png)

### API Développeur (Swagger)

L'API Apprise suit la spécification OpenAPI 3.0. Vous pouvez trouver le fichier `swagger.yaml` [à la racine du dépôt](https://github.com/caronc/apprise-api/blob/master/swagger.yaml).

</TabItem>
</Tabs>

## Étapes Suivantes

- **Apprenez la Syntaxe :** comprenez comment fonctionnent les [URL Apprise](/getting-started/universal-syntax/).
- **Configurez-le :** apprenez à utiliser les [Fichiers de Configuration](/getting-started/configuration/) pour gérer vos URL.
- **Trouvez des Services :** parcourez le catalogue des [Services Pris en Charge](/services/).
